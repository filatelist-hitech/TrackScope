[package]
name = "hitech-bpm-ffi"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "C ABI bridge boundary for Flutter/native integration"

[lib]
name = "hitech_bpm_ffi"
path = "src/lib.rs"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
hitech-bpm-dsp = { path = "../dsp" }
