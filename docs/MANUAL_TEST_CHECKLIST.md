# Ручной тест-чеклист — Phase 3 шаг 2

То, что автоматические тесты не могут проверить. Прогоните на железе перед мерджем Phase 3 мобильного моста в `stage`.

## Подготовка

- [x] Воркспейс собран: `cargo build --release -p hitech-bpm-ffi` из корня репозитория.
- [x] `flutter pub get` отработал чисто в `apps/mobile/`.
- [x] `flutter analyze` без ошибок.
- [x] `flutter test` зелёный (FFI + widget-тесты).
- [x] Железо: iPhone 11, iOS 26.3.1.
- [x] Опорное аудио: колонка, треки hitech-psytrance 192 / 200 / 207 BPM.

### Подготовка для iOS (дополнительные шаги)

- [x] `bash scripts/build_ios_native.sh` собрал `libhitech_bpm_ffi.a` для `aarch64-apple-ios`.
- [x] Xcode: Link Binary With Libraries → `apps/mobile/ios/Frameworks/libhitech_bpm_ffi.a`.
- [x] Xcode: Library Search Paths → `$(PROJECT_DIR)/Frameworks`.
- [x] Xcode: `OTHER_LDFLAGS` содержит `-force_load $(PROJECT_DIR)/Frameworks/libhitech_bpm_ffi.a`.
- [x] Podfile `post_install` содержит `PERMISSION_MICROPHONE=1` в `GCC_PREPROCESSOR_DEFINITIONS`.
- [x] `pod install` перезапущен после изменения Podfile.
- [x] iPhone: Developer Mode включён (Настройки → Конфиденциальность и безопасность → Режим разработчика).
- [x] iPhone: достаточно свободного места (≥ 500 МБ).

## Сборка и запуск

- [x] `cd apps/mobile && flutter run` стартует на целевом устройстве.
- [x] Появляется иконка приложения с названием «Hitech BPM Radar».
- [x] Нет краша на холодном старте.

## Сценарий разрешений

- [x] Первый запуск: появляется системный prompt разрешения микрофона.
- [x] Текст prompt'а на iOS содержит формулировку про «музыку вокруг» (соответствует `NSMicrophoneUsageDescription`).
- [x] При **grant**: приложение переходит на живой BPM-экран в течение ~1 секунды; индикатор BPM сначала показывает `— —` и обновляется по мере прихода аудио.
- [ ] При **deny** (мягкий отказ): приложение попадает на `PermissionDeniedScreen` с заголовком «Нужен микрофон для определения BPM». *(не тестировалось на этом прогоне)*
- [ ] Нажатие «Выдать доступ к микрофону» повторно запрашивает разрешение. *(не тестировалось)*
- [ ] При **постоянном запрете**: экран предлагает «Открыть системные настройки»; нажатие открывает системные настройки этого приложения. *(не тестировалось)*
- [ ] После включения разрешения в системных настройках и возврата в приложение `PermissionGate` перепроверяет. *(не тестировалось)*

## Путь живого захвата (с опорной колонкой)

Прогон 2026-05-26: треки 192/200/207 BPM, iPhone 11, расстояние ~1 м от колонки.

- [x] `lock_state` покидает `SEARCHING` в течение нескольких секунд.
- [ ] В течение 12 с `lock_state` показывает `STABLE`, `primary_bpm` в пределах ±2 BPM. ❌ **LOCKING** при 52–62% уверенности — `STABLE` не достигнут через комнатный микрофон (~1 м). На ~30 см не тестировалось отдельно.
- [x] Процент уверенности реагирует на сигнал и меняется.
- [x] Метр уровня входа в dBFS виден и реагирует: -10 до -12 dBFS на треках.
- [x] Spark-линия последних BPM обновляется.
- [x] Детектированный BPM: ~187–196 BPM на треках 192–207 BPM (ошибка ~±5–8 BPM — Phase 4 задача).

## Негативные / anti-fake точечные проверки

- [ ] Тишина (приглушить колонку, закрыть микрофон): `lock_state` НЕ достигает `STABLE`. `primary_bpm` возвращается к `— —`.
- [ ] Очень громкое воспроизведение (поднести телефон вплотную к колонке): `signal_quality.clipping` переходит в `true`, появляется красный чип `CLIPPING`, и `lock_state` не остаётся в `STABLE` бесконечно (`CLIPPED_MIC` допустим).
- [ ] Half-time-ловушка (играем 100 BPM kick): отладочный экран показывает raw-кандидат 100 BPM И нормализованный кандидат 200 BPM; в hitech-режиме основной должен предпочесть 200 BPM.

## Отладочный экран

- [ ] Нажатие на иконку bug открывает отладочный экран.
- [ ] Список кандидатов показывает ≥ 2 строки во время STABLE-воспроизведения.
- [ ] Каждая строка кандидата показывает BPM (2 знака после запятой), relation-лейбл (`main` / `half_time` / `double_time` / `raw` / `normalized_from_*`) и score.
- [ ] Блок signal-quality показывает каждое поле контракта DspResult (input_level_dbfs, peak_dbfs, clipping, clipped_frame_ratio, noise_level, snr_estimate_db, silence, breakdown_likely).
- [ ] Блок timing показывает растущий analysis_time_sec.
- [ ] Возврат на главный экран не перезапускает захват (BPM не сбрасывается обратно в `— —`).

## Плавность UI

- [ ] Непрерывный 60-секундный захват: UI остаётся отзывчивым; нет джанков при скролле отладочного экрана.
- [ ] Background → foreground: приложение продолжает с того места, где остановилось (состояние захвата может кратко проходить через не-`STABLE`).
- [ ] Force-quit и перезапуск: разрешение запомнено; живой экран появляется без повторного запроса.

## Известные платформенные особенности

- Android-эмулятор: микрофон хоста делится с эмулятором только при включённом AVD audio passthrough. Тест захвата может пройти, если опорное аудио играет с колонок хоста.
- iOS-симулятор: настоящего микрофонного пути нет; для строк живого захвата чеклист обязан запускаться на физическом iOS-устройстве.
- macOS (если когда-либо собирается как desktop-target): в настройках privacy → Microphone должно быть это приложение; иначе сценарий разрешений падает на `PermissionDeniedScreen`, несмотря на выданные Apple grants.

## Подписание

- [x] Использованное железо: iPhone 11
- [x] Версии ОС: iOS 26.3.1
- [x] Дата: 2026-05-26
- [x] Тестировщик: filatelist

**Итог прогона:** пайплайн конца в конец подтверждён. Разрешение микрофона, FFI-загрузка, DSP-воркер изолят и рендер UI работают. Точность ±5–8 BPM через комнатный микрофон — Phase 4 задача по сглаживанию и адаптивным порогам. Сценарии deny/permanent-deny не тестировались в этом прогоне.
